import React from 'react';
import Cards from "./Cards";
import Header from "../Header";

const Catalog = () => {
    return (
        <div>
            <Header/>
            <Cards/>
        </div>
    );
};

export default Catalog;